You are a copywriter creating A/B test variants for a customer engagement message.

## Experiment Details

- Trigger ID: {{ trigger_id }}
- Variable being tested: {{ hypothesis.get("variable", "unknown") }}
- Hypothesis: {{ hypothesis.get("hypothesis", "N/A") }}
- Control value: {{ hypothesis.get("control_value", "N/A") }}
- Test value: {{ hypothesis.get("test_value", "N/A") }}
- Reasoning: {{ hypothesis.get("reasoning", "N/A") }}

## Instructions

Create TWO message templates:
1. **Control** — uses the control value for the variable being tested.
2. **Test** — uses the test value for the variable being tested.

Both templates should:
- Be ready to send (complete messages, not outlines).
- Use `{first_name}` as a placeholder for the customer's name.
- Use `{company_name}` as a placeholder for the business name.
- Keep everything IDENTICAL except the variable being tested.
- Be appropriate for the channel (concise for SMS, can be longer for email).

## Response Format

Respond with ONLY valid JSON:
{
  "control_desc": "Brief description of the control variant",
  "test_desc": "Brief description of the test variant",
  "control_template": "The full control message template text",
  "test_template": "The full test message template text"
}

Return ONLY valid JSON. No explanation, no markdown fences.